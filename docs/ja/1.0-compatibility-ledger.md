# intent-cli 1.0 compatibility ledger

この reviewable ledger は
[1.0 compatibility promise](1.0-compatibility-promise.md) が指定する
v0.11.1 machine-consumed baseline を記録します。runtime behaviour の変更では
ありません。

## Command row の読み方

各 row は registered command/subcommand です。command/flag name、machine JSON
field semantics と machine が emit するすべての `cause` value、exit-code meaning、参照する
durable schema は 1.0 promise で covered です。「Command-scoped JSON」は common
envelope を発明しません。caller はその command の documented JSON shape と
その command が emit するすべての `cause` value を contract として扱います。存在しない `cause` field を inference
してはいけません。disposition は command surface に対する compatibility decision
です。

## Cause value の coverage

machine-consumed JSON payload で実際に emit されるすべての `cause` value は、prose がその value を現在 documented しているか、ledger が個別に列挙しているかにかかわらず covered です。coverage は prose documentation ではなく source emission で決まります。たとえば automation が受け取れる `topology-location-conflict` は covered です。

| Registered command | Machine JSON と cause vocabulary | Exit code | Durable schema / disposition |
| --- | --- | --- | --- |
| `automation branch-lane-propose-record` | design が actor、timestamp、rationale、evidence、definition revision、fingerprint を持つ lane propose record を記録する preview-through-1.x の command-scoped JSON。 | preview または新規 write は `0`。fact 欠落、routing 不正、既存の矛盾する record は nonzero。 | G669; `preview-through-1.x` |
| `automation branch-lane-confirm-record` | orchestration が独立した actor、timestamp、evidence と proposal fingerprint を持つ confirm record を記録する preview-through-1.x の command-scoped JSON。 | preview または新規 write は `0`。propose 不在、mismatch、malformed、同一 actor は nonzero。 | G669; `preview-through-1.x` |
| `automation base-branch-check` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `automation check` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `automation ci-wait` | durable な exact-head wait の record/clear/show を提供する preview-through-1.x の command-scoped JSON。moved head は old result の再利用ではなく advancing repair として扱い、report delivery は `recipient_warning` を emit できます。 | valid な read/plan/write は `0`。malformed または読めない durable state は nonzero。 | `preview-through-1.x` |
| `automation continuation-chain` | domain/team ごとの append-only completion chain を読む read-only の command-scoped JSON / Markdown。task、completion signal、chain id で絞り込み、timestamp 付き全 link と正確な `next_missing_link` を返す。 | readable な store（empty store を含む）は `0`。不正な引数または durable state の unreadable は nonzero。 | G695; `preview-through-1.x` |
| `automation clarification-stop` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `automation closeout-drift-check` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `automation complete` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `automation doctor` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `automation durable-state-preflight` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `automation heartbeat` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `automation host-loop-next-action` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `automation host-loop-wake` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `automation host-queue-item-recovery` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `automation host-review-diagnostics` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `automation host-review-preflight` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `automation host-sync-preflight` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `automation intent-target-gap-recovery` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `automation issue-block` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `automation issue-publish` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `automation issue-release` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `automation issue-retire` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `automation knowledge-writeback-record` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `automation guide-reachability-record` | packet が宣言した guide surface と routing role を記録する preview-through-1.x の command-scoped JSON。route は推測せず、guide wording も判定しない。 | valid な read/plan/write は `0`。宣言がない、malformed、record evidence が読めない場合は nonzero。 | 下の durable inventory を参照。 `preview-through-1.x` |
| `automation label-palette-audit` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `automation label-palette-sync` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `automation pr-transition` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `automation publish-lifecycle-repair` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `automation publish-recovery` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `automation queue-dependency-reconcile` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `automation queue-seed-from-packet` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `automation reconcile` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `automation runs-audit` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `automation same-repo-metadata-preflight` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `automation stalled-work` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `automation state-doctor` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `automation summary` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `automation workspace-guard` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `bug implementation-issue` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `bug implementation-repair` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `bug plan` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `bug report` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `bug triage` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `clarification answer` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `clarification next` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `clarification status` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `clarify answer` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `clarify draft` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `clarify list` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `clarify open` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `clarify record` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `closeout pr` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `claim acquire` | preview command-scoped JSON。plain push 成功後だけ `acquired`。`held` は origin holder を名指しし、unrelated advance は bounded fresh-base reapply を行う。 | plan/acquired は `0`。held、retry exhausted、validation/Git failure は nonzero。 | G679; `preview-through-1.x` |
| `claim release` | actor/team/reason を明示した release push の preview command-scoped JSON。 | plan/released は `0`。claim absent、holder mismatch、validation/Git failure は nonzero。 | G679; `preview-through-1.x` |
| `claim takeover` | displaced holder を名指しし actor/team/reason を明示した takeover の preview command-scoped JSON。 | plan/taken-over は `0`。claim absent、displaced-holder mismatch、validation/Git failure は nonzero。 | G679; `preview-through-1.x` |
| `claim stranded` | 設定済み metadata branch に取り残された active claim の report と、resolved canonical branch への明示確認付き dry-run/write migration を返す preview command-scoped JSON。conflict では canonical record を上書きせず、通常の verify は canonical-only のまま。 | 読み取り可能な report、clean、planned、または成功した migration は `0`。evidence 不在、確認/参照不正、conflict、Git failure は nonzero。 | G763; `preview-through-1.x` |
| `context collect` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `guide artifact-intake` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `guide automation` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `guide closeout` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `guide collaborate` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `guide commands` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `guide design-thread` | agent kind に依存しない design-thread operating contract の Markdown または command-scoped JSON。 | valid な render は `0`。documented な argument error は nonzero。 | G654。 `preview-through-1.x` |
| `guide bootstrap` | read-only な application-front-door bootstrap の Markdown または command-scoped JSON。human への seat / app-kind question、既存 herdr-only setup command の compose、partial / join state の命名、explicit handoff を扱う。 | valid な render は `0`。documented な argument / state error は nonzero。herdr、OS、scheduler、provider、application operation は実行しない。 | G664。 `preview-through-1.x` |
| `guide grill` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `guide help` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `guide host-ownership` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `guide improve` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `guide inspect` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `guide intent-work` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `guide interview-mode` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `guide interview-readiness` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `guide model` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `guide next` | documented な `--format json` form がある場合は command-scoped JSON。optional `--role` は invoking role の最初の read-before-acting contract pointer と attributed G672 incident record を追加し、machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。base command は `stable-at-1.0`、G672 pointer/incident output は `preview-through-1.x` |
| `guide onboarding` | documented な `--format json` form がある場合は command-scoped JSON。optional `--role` は invoking role の最初の read-before-acting contract pointer と attributed G672 incident record を追加するが、first-call sequence は変更しない。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。base command は `stable-at-1.0`、G672 pointer/incident output は `preview-through-1.x` |
| `guide oneshot` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `guide orchestrator-thread` | documented な `--format json` form がある場合は command-scoped JSON。herdr-only wake-source guidance は `pane.agent_status_changed` の concrete な `--event-mode` flag を示す。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。base command は `stable-at-1.0`、G672 wake-source pointer は `preview-through-1.x` |
| `guide prompt-matrix` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `guide prompt-template` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `guide question-style` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `guide review` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `guide review-verification-policy` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `guide rules` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `guide seat-commands` | 選択した Claude/Codex seat kind の versioned command-scoped JSON または Markdown。sanctioned form、prefix-matching breaker、実測済み alternative を表示し、settings の変更や command の approve はしない read-only surface。 | 有効な kind/format は `0`。kind 不足または未対応 argument は nonzero。 | G696; `preview-through-1.x` |
| `guide topology-workspace-move` | operator-supplied topology workspace rebuild recipe の read-only command-scoped JSON または Markdown。inspect、full before/after preview、explicit CAS-guarded write、validation、notify preflight を表示する。 | valid な render は `0`。documented な argument error は nonzero。 | G697; `preview-through-1.x` |
| `guide skill-pack` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `guide stack` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `guide start` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `guide steward-thread` | Steward の metadata-free operating contract を示す read-only Markdown または command-scoped JSON。reader path、単独で行える action、architect/reviewer/orchestrator への handoff、refusal/report route、live-pair dialog path、working-tree discipline、G796 ruling boundary を含む。 | valid な render は `0`。documented な argument error は nonzero。 | G807; `preview-through-1.x` |
| `guide worker` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `guide workflow` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `intent add-feature` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `intent analyze-tree` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `intent draft-from-interview` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `intent explain` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `intent facet-check` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `intent host-check` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `intent init` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `intent init-tree` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `intent lint-layout` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `intent next-slice` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `intent search` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `intent status` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `interview answer` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `interview compile` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `interview next-question` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `interview record-answer` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `interview resume` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `interview start` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `issue create` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `issue draft` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `issue plan-candidate` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `issue prepare` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `issue publish` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `issue publish-flow` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `issue publish-reviewed` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `issue status` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `issue validate-body` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `metadata update` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `metadata validate` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `migrate host-state` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `next-slice classify` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `notify adjudicate` | canonical capability-checked prompt surface の preview-through-1.x command-scoped JSON。root の `exit_code` と nested result は `decision`、rule、declared actor、mechanical executor、`answerable_by`、risk tag、matched scope、live pane/state-sequence/text-hash evidence を返す。G689 の owned-scratch-delete は recorded current/last cycle identity を使い、caller の identity を未検証のまま使わない | accepted dry-run または監査済み bounded execution の成功だけが `0`。validation、capability / hard-floor を含む refusal、stale cycle / CAS、live/state/policy の unreadable、audit/write failure、key execution failure は nonzero | 下の durable inventory を参照。 `preview-through-1.x` |
| `notify collect` | persisted undelivered report entry を一つ再送する preview-through-1.x の command-scoped JSON。already-delivered task は拒否し、work を re-delegate しない。 | collected report は `0`。missing、unreadable、already-delivered の outbox/pending state は nonzero。 | 下の durable inventory を参照。 `preview-through-1.x` |
| `notify reconcile` | orchestration が delivered sender-local report を host 側で消費する preview-through-1.x の command-scoped JSON。host pending の report と `report-received` continuation link を最大 1 回だけ append し、replay は already-converged として返す。child は host state を write しない。 | dry-run、初回 reconcile、idempotent replay は `0`。local handoff の missing / non-delivered、host state の conflict、store の unreadable / write failure は nonzero。 | G719。`preview-through-1.x` |
| `notify delegate` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `notify dispose` | `superseded` または `applied-elsewhere` として open pending delegation を明示的に settle する preview-through-1.x の command-scoped JSON。actor、timestamp、reason、該当する evidence を持ち、遅い report は配信して disagreement を示す。 | dry-run または新しい disposition は `0`。evidence 不足、unknown task id、already-settled、pending state の read/write failure は nonzero。 | 下の durable inventory を参照。 `preview-through-1.x` |
| `notify escalate` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `notify report` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `notify research-status` | issued research delegation 数と delegation なしの judgement-seat turn 数だけを説明的に返す、preview-through-1.x の read-only command-scoped JSON。grading、refusal、recipient contact は行わない。 | readable な aggregate（empty store を含む）は `0`。不正な引数または research store の unreadable は nonzero。 | G800; `preview-through-1.x` |
| `notify status` | documented な `--format json` form では `dispatched_at`、recorded recipient identity、`recipient_running`、`process_present`、`liveness_state`、名前付き liveness source、residency-resolved な `delivery_basis`、`report_arrived`、`settlement_basis`（`open|report|disposition`）、disposition、late-report disagreement、`verdict`（`live|settled|lost|registration-lost-process-present`）を返し、machine が emit するすべての `cause` value を covered にする。 | pending store / process corroboration を解決できた status（registration loss と disposition-settled record を含む）は `0`。解決できない場合は nonzero。 | 下の durable inventory を参照。 `preview-through-1.x` |
| `notify supervise` | bounded / measured supervision の command-scoped JSON。recipient-lost、registration-lost-process-present（recorded pane ごと cycle ごとに 1 finding）、actionable terminal CI、queue/write-back、recorded-seat、undelivered-escalation、declared bound、recovery duration、supervisor liveness を自動 lifecycle transition なしで返す。G657 は owner-subject finding を design へ 1 件の escalation として route する。G658 の `supervise install` は emit-only scheduler authoring subcommand。G659 は opt-in `--event-mode` を追加し、recorded seat ごとの blocking herdr wait を同じ process に保持して failure 後 re-arm し、独立 interval floor と de-dup する。G672 は `pane.agent_status_changed` を normative SECOND wake source と説明する help を追加するが event behavior は変えない。 | healthy / bound 内または検証済み pass（registration loss の distinct state を含む）、または scheduler artifact の emit / preview 成功は `0`。recovery / wake の fail-closed 停止、declared bound 超過、artifact authoring failure は nonzero。install は scheduler command や process-management command を実行しない。 | 下の durable inventory を参照。 `preview-through-1.x` |
| `judgment-wait open` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `judgment-wait query` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `judgment-wait resolve` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `judgment-wait supersede` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `operator-attention open` | alias の JSON は replacement `judgment-wait` を示す structured `deprecation_warning` field 以外、`judgment-wait open` と同一。 | `judgment-wait open` と同じ。 | `deprecate-with-alias`; 1.x を通じて保持し removal は next MAJOR のみ。 |
| `operator-attention query` | alias の JSON は replacement `judgment-wait` を示す structured `deprecation_warning` field 以外、`judgment-wait query` と同一。 | `judgment-wait query` と同じ。 | `deprecate-with-alias`; 1.x を通じて保持し removal は next MAJOR のみ。 |
| `operator-attention resolve` | alias の JSON は replacement `judgment-wait` を示す structured `deprecation_warning` field 以外、`judgment-wait resolve` と同一。 | `judgment-wait resolve` と同じ。 | `deprecate-with-alias`; 1.x を通じて保持し removal は next MAJOR のみ。 |
| `operator-attention supersede` | alias の JSON は replacement `judgment-wait` を示す structured `deprecation_warning` field 以外、`judgment-wait supersede` と同一。 | `judgment-wait supersede` と同じ。 | `deprecate-with-alias`; 1.x を通じて保持し removal は next MAJOR のみ。 |
| `packet draft` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `packet retire` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `project status` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `projection generate` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `projection regenerate` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `prompt-class describe` | 1 つの recorded `agent-kind:prompt-class` を示す read-only registry JSON/Markdown。shell class は extracted payload と shipped scope の metadata を示すが、policy を記録せず dialog に回答しない。 | recorded class は `0`、selector 不正または class 不在は nonzero。 | 下の durable inventory を参照。 `preview-through-1.x` |
| `prompt-class list` | recorded prompt class と scoped policy 要否を列挙する read-only registry JSON/Markdown。 | inventory の描画は `0`、format または selector 不正は nonzero。 | 下の durable inventory を参照。 `preview-through-1.x` |
| `queue dispatch` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `queue enqueue` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `queue list` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `queue next` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `queue priority-drift` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `queue reprioritize` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `queue show` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `queue transition` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `review closeout-plan` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `review collect-signals` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `review signal-handled` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `safety nested-provider-handoff` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `session-layer inspect` | preview-through-1.x の read-only observation。記録済み role の residence/kind/frontend と herdr の live workspace/pane/agent state を返し、明示的 role の bounded pane tail も扱う。focus、prompt、key-send、dialog、process-management は行わない。topology/session-layer が無い、または失敗した場合は記録済み field と unavailable reason を保持して `0` で返す。 | observation の描画（session-layer unavailable を含む）は `0`。引数不正または unknown role は nonzero。 | G790; `preview-through-1.x` |
| `session-layer seat` | G808 の read-only `preflight` seat-start probe。`.git` writable、remote 到達性、installed identity、host claim path、generic runtime-family marker の五つを確認し、host-local result を一件記録する。index、branch、sandbox、process、remote は変更せず、probe/family ごとの remedy line を返す。 | 五 probe と ledger append が全て成功した場合だけ `0`、それ以外は nonzero。 | G808; `preview-through-1.x` |
| `session-layer marker` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `session-layer model-resolution` | host-local な verified/refused launch evidence の append/query と明示的な negative retry prevention を返す preview command-scoped JSON。provider operation はない。 | valid な dry-run/write/query は `0`。validation、unsupported grammar、unreadable/write-failed ledger は nonzero。 | G685; `preview-through-1.x` |
| `session-layer set` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `session-layer show` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `session-layer team-mode` | G691 durable team-mode show/set/validate surface の alias。command-scoped JSON は `delivery|authoring-only` と transition trail を保持する。 | valid な read/plan/write/validate は `0`。malformed state、invalid argument、not-applicable delivery operation の拒否は nonzero。 | G691; `preview-through-1.x` |
| `session-layer topology` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。G697 の `move` subcommand は explicit-write、dry-run-first、CAS-guarded である。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。record/read surface は `stable-at-1.0`、G697 move は `preview-through-1.x` |
| `team-mode show` | durable な `delivery|authoring-only` team shape、source、明示的な transition trail を返す preview-through-1.x command-scoped JSON。 | valid な read は `0`。malformed state または invalid argument は nonzero。 | G691; `preview-through-1.x` |
| `team-mode set` | 明示的で reversible な `delivery|authoring-only` transition の preview-through-1.x command-scoped JSON。write には `--write` が必要。 | valid な plan/write は `0`。malformed state または invalid argument は nonzero。 | G691; `preview-through-1.x` |
| `team-mode validate` | team-mode state validation の preview-through-1.x command-scoped JSON。 | valid state は `0`。malformed state または invalid argument は nonzero。 | G691; `preview-through-1.x` |
| `skill diff` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `skill install` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `skill list` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `status brief` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `task fix-pr-comments` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `task issue-to-pr` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `task publish-next-issue` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `task review-pr` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `tasking ai-thread-summary-attach` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `tasking handoff` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `tasking handoff-bundle` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `tasking handoff-bundle-history` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `tasking handoff-bundle-import-dry-run` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `tasking handoff-bundle-inspect` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `tasking handoff-bundle-verify` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `tasking publish-reviewed-bridge` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `tasking task-packet` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `tasking task-packet-checklist` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `tasking task-packet-preview` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `worker claim` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `worker complete` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `worker issue-preflight` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `worker next-action` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `worker pr-comment-preflight` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `worker pr-review-preflight` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `worker result-summary` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |
| `worker signal` | documented な `--format json` form がある場合は command-scoped JSON。machine が emit するすべての `cause` value が covered であり、cross-command inference はしない。 | success は `0`。それ以外は command の documented な nonzero validation/operational result。 | 下の durable inventory を参照。 `stable-at-1.0` |

## Durable schema と legacy inventory

freeze 後の surface を ledger から省略してはいけません。preview surface は
`preview-through-1.x` で列挙します。registered command ごとの row を要求する同じ completeness
guard により、preview も発見可能なままになります。

| Surface | Machine-consumed shape / state | Disposition | Evidence / rule |
| --- | --- | --- | --- |
| session-layer mode record | `.intent-cli/session-layer-mode.json`; recorded mode と transition history | `stable-at-1.0` | state transition semantics は covered。 |
| team mode record | `.intent-cli/team-mode.json`; domain/team-scoped な `delivery|authoring-only` と command-produced transition history。absence は `delivery` に解決し、`--team` 省略時は一意な team-scoped record を使い、曖昧さは `team-mode-ambiguous` で fail-closed する。 | `preview-through-1.x` | G691/G692。team shape は session-layer transport と直交する。authoring-only は delivery topology、supervisor、worker lifecycle を持たず、malformed / ambiguous record は fail-closed。 |
| authoring-only publish audit と external handoff | `issue publish-flow` が design actor、operator-acceptance evidence、destination ownership と `.intent-cli/published-external-handoff/<execution-unit>.json` を記録し、matching issue/destination record だけが `published-not-delegated` を抑制する。 | `preview-through-1.x` | G692。既存の readiness、claim、content、duplicate、branch-lane gate は必須のまま。delivery の issue/run-event byte は不変。lane confirmation は design + distinct operator で、orchestration impersonation はしない。named worker への `notify delegate` は nonzero の `not-applicable-team-mode` で拒否する。 |
| shared team-mode capability matrix | `automation stalled-work`、`automation state-doctor`、`intent status`、`status brief` が同じ authoring-only active/not-applicable capability class を公開し、delivery は全 matrix を保持する。 | `preview-through-1.x` | G692。worker、review、CI、delegation、supervisor は authoring-only でのみ not applicable。authoring、contract/readiness、branch-lane、branch-routing、publish-durable-state-drift、knowledge/guide-writeback は active のまま。 |
| per-team topology record | canonical team topology record と identity/path validation。role ごとの optional な bounded free-form operator-declared `model` / `reasoning_effort` field を含み、render された value は operator declaration であって measurement ではなく、absent field でも valid のまま。 | `stable-at-1.0` | G739。record-first、inferred mode change はしない。これらを省略した legacy record は exact round-trip で byte-compatible。 |
| topology host-state declaration | `session-layer topology record-host-state` が `host_state.role` と `host_state.envelope` を記録し、declaration のない legacy record には informational な `host-state-role-missing` finding を出す。guide orchestrator-thread は明示された route を discovery する。authority は residence、kind、external placement、co-location から推測しない。 | `preview-through-1.x` | G736。legacy topology は valid のままで migrate されないが、実際に capable な participant がない team は host-state workflow work を実行できない。design role の明示的 declaration は正当で、undeclared / ad-hoc request だけを禁止する。 |
| topology workspace move | `session-layer topology move` は operator が指定した old-pane=new-pane map により、recorded team workspace と全 herdr role の workspace/pane pair を更新する。同じ old pane を共有する role はその一つの new pane へ一緒に移動し、異なる old pane の同じ new pane への集約は拒否する。full before/after preview、unrelated JSON field の preservation、stale CAS snapshot の refusal も提供する。 | `preview-through-1.x` | G697/G735。operator-supplied transition のみ。herdr query、workspace discovery、pane provisioning、membership change、per-role repair はしない。record-side workspace mismatch はこの whole-team path を示す。 |
| judgment-wait record | `.intent-cli/operator-attention.json`; `open` → `resolved` または `superseded` | `stable-at-1.0` | G623 が general な durable record を命名し、on-disk format と identifier は変えない。 |
| operator-attention command alias | `judgment-wait` の alias。record schema と identifier は変更しない。 | `deprecate-with-alias` | structured `deprecation_warning` が replacement を示し、alias は 1.x を通じて残り removal は next MAJOR のみ。 |
| role-pane-mapping compatibility read | removed になった legacy fixed-file read。legacy-only host は record/retire recovery command を示して fail-closed する | `retire-before-1.0` | retirement evidence: Zero4Racer の `legacy-topology-retirements.jsonl` entry one と、legacy-free を確認した 4 host。 |
| pending delegation record | `.intent-cli/notify/<domain>/<team>/pending.jsonl`。task id を key に、recipient identity、expected artifact、dispatch timestamp、report resolution、または明示的な `superseded` / `applied-elsewhere` disposition metadata を持つ append-only dispatch/report/disposition snapshot | `preview-through-1.x` | G629/G630/G671。v0.12.0 freeze 後の追加。disposition は command のみで作成し、disposed record は supervision / stalled-work の open 集計から外し、遅い report は message carriage として保持する。 |
| report outbox record | `.intent-cli/notify/<domain>/<team>/report-outbox.jsonl`。task id を key に、result nonce、payload、delivery state/error、timestamp を持つ sender-side の append-only report snapshot | `preview-through-1.x` | G653。v0.12.0 freeze 後の追加で 1.0 promise の対象外。report durability、manual collection、undelivered-outbox supervision を含む。 |
| completion continuation-chain record | `.intent-cli/continuation-chains/<domain>/<team>/chains.jsonl`。completion signal ごとの append-only snapshot で、`report-received`、`orchestration-wake-attempted`、`wake-delivered-or-observed`、`canonical-state-classified`、`required-continuation-started` または `named-blocker-recorded` を link する | `preview-through-1.x` | G695。report delivery と observed working→settled transition が chain を開始し、read-only query surface が正確な missing link を出す。chain は observation だけを記録し、lifecycle transition は実行しない。 |
| improve-run record と realignment window | `.intent-cli/improve/<domain>/runs.jsonl` の append-only record は `domain`、`mode`、`recorded_at`、`touched_artifacts` を持つ。独立した `window.json` は supervision bound と同様に team の `window_days` 宣言を持つ | `preview-through-1.x` | G662。human / agent の realignment work が実施された事実だけを記録する。`guide next` は timestamp recency と独立して declared された window だけを比較し、quality を grade しない。scheduler、auto-run、stalled-work class は追加しない。 |
| measured supervision records | `.intent-cli/supervision/<domain>/<team>/bound.json`、`cycles.jsonl`、`stalls.jsonl`。declared detection bound、completion 起点の restart/liveness gap、cadence と headroom の threshold、stall ごとの detectable/surfaced/cleared timestamp を記録し、unknown start は null のまま保持 | `preview-through-1.x` | G641。v0.12.0 freeze 後の追加で 1.0 promise の対象外。measured supervision、transport wake、undelivered-escalation detection、recovery duration evidence を含む。declared bound がない場合は cadence を self-liveness にだけ使い、`max(2 * cadence, cadence + 60s)` の headroom を設け、`bound_met` は null のままにする。`gap_seconds` は直前の completion から現在の開始までを測る。Delivered になった escalation record は clear し、後続 finding を繰り返さない。 |
| supervisor writer identity と duplicate detection | 新しい `cycles.jsonl` cycle は `writer.pid`、`writer.process_start_time`、`writer.host` を追加する。different live writer の recent cycle は cycle ごとに 1 件の `duplicate-supervisor` finding を出し、両 writer、other-cycle age、duplicate-wake cost、G658 team scheduler label の remedy を明示する | `preview-through-1.x` | G676。legacy record は readable のままで finding を出さず、dead / stale / same-writer cycle も finding を出さない。detection のみで kill/stop/rank/election、lock、lease、duplicate-seat detection は追加しない。install guidance は restart 後に残る stale hand-run supervisor を operator が整理することを示し、2026-08-12 の measured attribution は 1 team の 4 concurrent loop である。 |
| per-team residual pre-approval policy producer と audited execution | `.intent-cli/supervision/<domain>/<team>/pre-approval-policy.json` は validated かつ non-overlapping な accept/escalate pair と rule ごとの producer applicability を保持する。dialog-blocked seat は bottom detection snapshot の current trailing dialog だけを分類し、stale-known/current-unknown text は class `unknown` で escalate-only。prompt audit は既存 `cycles.jsonl` に append する。 | `preview-through-1.x` | G666/G682/G683/G690。unknown または conflicting pair は拒否され、uncovered kind は `inapplicable-no-prompt-class-producer` を維持し、legacy overlap では escalation が優先される。matched pre-approve は shared adjudication pipeline に入り、exact keys の前に authorization と execution-pending を永続記録して terminal outcome を記録する。`answerable_by` は exact class と matched scope から解決し、canonical `notify adjudicate` は pane/state-sequence/text-hash CAS と decision actor / mechanical executor の分離を要求する。未解決 pending は reconciliation-required で再試行しない。pre-escalate、unmatched、unknown、capability-denied は answer を実行しない。hard floor は `destructive`、`credential`、`permission-change`、`security`、`product-decision`、`unverifiable`。この slice の shipped class/scope に design-answerable はない。fuzzy matching、generic relay、cross-team policy は追加しない。 |
| shell prompt vocabulary と scoped policy instance | `codex:shell-command` は recipe-backed class として shell payload を抽出し、`.intent-cli/supervision/<domain>/<team>/pre-approval-policy.json` は `project-test`、`owned-scratch-delete`、`exact-command-once` の validated `scoped_policies` を保持する。監査 prompt record は matched scope、normalized command digest、dialog hash、terminal outcome を持つ。 | `preview-through-1.x` | G689/G690。compound shell-AST の全 segment は scoped instance に一致しなければならない。`project-test` は test execution で read-only ではない。`owned-scratch-delete` は同じ wake の scratch ledger の exact path と current wake identity を要求し bare `/tmp` を拒否する。`exact-command-once` は digest/hash を 1 回だけ使う。unknown syntax、substitution、redirect、root 外 path、uncovered segment、persistent provider option、bare `--pre-approve codex:shell-command` は fail closed。`answerable_by` と risk tag は class/scope capability resolution に含まれるが、shipped shell scope は orchestration-only。`prompt-class list|describe` は inspection-only、design は canonical surface と live CAS を bypass できない。 |
| seat ごとの launch-envelope recipe drift | 各 supervision cycle は running seat ごとに structured argv と recorded kind recipe を sandbox mode、approval mode、writable roots/add-dirs、network access について比較する。`recipe-drift` は両 shape を示し、bound の欠落/拡大には `recipe-envelope-alarming`、narrowing には informational な `recipe-envelope-narrower` を使う。 | `preview-through-1.x` | G684。model と reasoning effort は operator-choice の wish field として除外する。conforming または wish だけが異なる seat は無出力で、persistent drift は cycle ごと seat ごとに 1 回出るが、誰も wake せず、restart、repair、kind change、prompt adjudication その他の action を行わない。 |
| operator-recorded typed envelope profile | `session-layer topology record-profile` は confirmation と current-digest CAS により kind ごとの named profile を記録する。role の reference または typed override は G684 registry より優先し、profile がない場合は registry comparison を byte-identical に保つ。dangling reference と kind mismatch は distinct な `profile-invalid` finding とし、暗黙の fallback はしない。 | `preview-through-1.x` | G686/G628。profile field は argv から学習せず operator が記録する fact。`update-kind`、`update-field`、recovery、launch、cadence、G647 registry limit、G684 wish exclusion は変更しない。 |
| registration-loss process corroboration | herdr が exact recorded workspace/pane で running registration を失っても foreground process が残る場合の `registration-lost-process-present`。status は `liveness_state` / `process_present` を返し、supervision は pane ごと cycle ごとに 1 finding、delivery は同じ cause と `resend_permitted: true` を返す | `preview-through-1.x` | G648。v0.12.0 freeze 後の追加。process corroboration がない場合だけ genuine `lost` とし、kill / restart / automatic re-registration は行わず、G630 fail-closed recovery を変更しない。 |
| sandbox report routing と registration executability/actionability | implementation report は `--routing-root <host-routing-root> --report-root .` を使い、host root は read/transport authority、role root は sender-local outbox とする。`agent_session=missing` と、記録済み pane の既存 seat へ 1 回 no-op prompt を送ってから bounded check を再実行する operator act を診断に含める | `preview-through-1.x` | G719。2 つの measured surface は 1 つの executability/actionability class。host-root write grant、retry、hand-written transport、automatic re-registration、restart、kill、boundary widening は行わず、active registration は ownership stop のまま。 |
| guide reachability records | `.intent-cli/guide-reachability/<unit>/record.json`。packet の guide surface、routing role、target surface、host commit evidence を持ち、record ができるまで `stalled-work` は `guide-reachability-pending` を出す。明示的な no-surface は silent。 | `preview-through-1.x` | G645。v0.12.0 freeze 後の追加。declaration は推測せず、absent と explicit no-surface を区別し、debt は merge/closeout を block しない。 |
| public project feedback guidance | `guide feedback` は public な `J-Tech-Japan/intent-system` issue channel、reviewed な `gh issue create` form、permanent public-data warning、recommended report shape、human/design の per-action filing boundary を JSON/Markdown で render する | `preview-through-1.x` | G705。render-only であり、issue creation、GitHub/API POST、network connection、subprocess、confirmation submission、telemetry write/queue、child-issue publishing は行わない。AI seat は draft のみで、design/operator が deliberate に file する。 |
| role-scoped closeout records | compatibility の `record.json` は unattributed な legacy record として readable のまま保持する。明示的な recorder role は `.intent-cli/{knowledge-writebacks,guide-reachability}/<unit>/records/{design,orchestration}.json` に保存し、read/result surface は全 role を列挙する。`stalled-work --role <role>` は指定 role だけを clear する | `preview-through-1.x` | G698。orchestration は mechanical closeout、design は intent-tree/guide lesson を担当する。同じ role の duplicate/conflicting evidence は従来通り拒否し、自動 migration / legacy rewrite はしない。 |
| repeated-observation emission hygiene | `notify supervise` は resolved full cadence、repeat backoff、pane-status debounce を `.intent-cli/supervision/<domain>/<team>/emission-policy.json` に記録する。active same-key record は `first_seen`、`last_seen`、`repeat_count`、`parked`、`emission_cadence_seconds` を表示する | `preview-through-1.x` | G699。repeated finding は黙って drop せず parked にし、resolution / fingerprint change で record を reset し、新しい key は即時のままにする。settled pane status は記録された consecutive threshold だけで分類する。detection predicate、G695 continuation-chain record、observation-only authority、既存 JSON field は不変。 |
| same-cycle supervision corroboration | `registration-lost-process-present` と `live-idle-no-report` は同じ cycle の non-terminal な seat observation を参照する。producer が矛盾した場合は named な `observation-conflict` を 1 件出し、`registration_definition`、`registration_lookup`、`registration_result`、`consulted_observations` を保持する | `preview-through-1.x` | G707。inconclusive result は verification first で automatic action を許可しない。矛盾する observation がない genuinely absent は従来通り候補であり、conflict key には G699 の backoff/park を適用する。canonical workflow の ownership と observation-only authority は不変。 |
| closeout の runs 書込み事実と明示的な修復 | `closeout pr` は実際に `runs.jsonl` へ append した内容だけを出力し、skip では `runs_events: []`、`runs_appended: false`、named な `runs_skip_reason` を示し、append 時は実際に追加した行だけを `runs_appended: true` で示す。queue-completed の欠落は `queue-completed-missing-closeout-runs-events` として named にする | `preview-through-1.x` | G708。`--repair-runs --write` は明示的な opt-in で、欠落した closeout event だけを追加し、再 completed、queue-state/他 record の書込みを行わず、queue-state bytes を保持し、二回目は no-op になる。guide orchestrator-thread は metadata-free directory から JSON/Markdown contract を render する。 |
| named branch-lane registry と routing snapshot | domain ごとの `[project.branch_lanes.<domain>]` 定義と、draft、queue seed、projection regeneration、review guidance、worker issue-body base-branch check を通る packet の `branch_lane`、`branch_lane_source`、immutable `routing_snapshot`、および optional queue-item `routing_snapshot` projection | `preview-through-1.x` | G668。一致する domain registry がない場合は legacy policy adapter と byte 単位の draft 出力を維持し、queue snapshot は registry / packet 編集後も accepted packet を retarget しない。 |
| lane decision record と routing stall classification | `.intent-cli/branch-lane-decisions/<unit>/{propose,confirm}.json`。actor、timestamp、evidence、rationale/fingerprint を分離して保持し、publish refusal と `branch-lane-decision-pending` / `branch-routing-conflict` を提供 | `preview-through-1.x` | G669。lane packet は独立した両 record を要求し、legacy packet は gate の対象外。pending は queued work の age gate、conflict は closed PR を含めて即時に全 routing value を列挙する。 |
| shared publish-gate readiness exclusion | publish gate、`intent next-slice`、`automation stalled-work` が 1 つの readiness judgment を参照する。拒否された packet は既存の next-slice `notes` と stalled-work `excluded[]` channel に `reason: contract-incomplete` として unit と cause を出し、packet の完成後は自動的に candidacy へ戻る。 | `preview-through-1.x` | G670。command、stall kind、marker、packet field は追加しない。stable command contract は維持し、この新しい named exclusion behavior だけを preview とする。 |
| orchestrator の delegation 前 prerequisite guidance | `guide orchestrator-thread` の Markdown/JSON contract。orchestrator-owned workspace preparation、recipe write-envelope check、prepare-and-resume permission recovery を扱う | `preview-through-1.x` | G655。v0.12.0 freeze 後に追加した guidance-only surface。新 command、git execution、envelope widening、recovery kind change はない。 |
| design-thread operating guidance | `guide design-thread` の Markdown/JSON contract。4 outcome wake、provenance、transaction approval、merge comparison、delegation verification、duty separation、outcome report を扱う | `preview-through-1.x` | G654。guide-only surface であり、workflow mutation、terminal parse、provider launch、新しい model-backed monitoring role はない。 |
| guide primacy と registry-backed herdr setup | `guide orchestrator-thread` は `herdr-standard-layout/v1` を render し、team tab 1 つ、左 orchestration、右上 implementation、右下 review、role label、exact creation command、measured `herdr pane move --tab --split right|down --target-pane` / `herdr pane rename` repair、non-blocking `layout-and-labels` check を示す。setup と `guide design-thread` は `dialog-answering/v1` を render する | `preview-through-1.x` | G701。guide surface は primary interface であり、missing/wrong/stale route は operationally unshipped、route 実行は functional test と同等の acceptance substance、G645 record が reachability を enforce する。read-only で herdr/provider/terminal mutation はない。 |
| role-contract-first guidance と incident record | `guide next --role <role>` と `guide onboarding --role <role>` は design/orchestration/implementation/review のみ installed contract pointer を先頭に置き、既存 procedure と first-call sequence を維持し、issue #1441 sections D/B-1 の attributed remote-herdr incident を記録する | `preview-through-1.x` | G672。guidance/rendering のみ。contract rewrite、wake ごとの強制 reread、event behavior 変更、新 workflow surface、B-1 の解決はない。 |
| named GitHub quota degraded state と partial detection | GitHub-consulting surface は exhausted resource と reset を持つ `cause: github-api-quota-exhausted` を emit し、`stalled-work` は local-only finding を `partial` と `detection_available: false` で保持する。worker / host surface は unavailable と empty を区別し、doctor は resource ごとの remaining/reset と non-`ok` quota verdict を返す | `preview-through-1.x` | G673。structured rate-limit observation のみ。retry、wait scheduler、request budget、transport migration、cache、batching、wake-source change、supervision-class change は追加しない。remote-herdr #1442 measurement と同日 host G667 corroboration は別 attribution のまま保持する。 |
| 検証済み REST GitHub issue read と named GraphQL remainder | #1442 で測定した 6 surface は field-complete な issue-list projection（`number`、`title`、`html_url`、`created_at`、`body`、`updated_at`、`labels[].name`、`state`。REST の `pull_request` marker は issue filter のみ）を `gh api` REST で読む。未検証の `closingIssuesReferences` を消費する PR read と、stalled-work の `CheckRun`/`StatusContext` rollup は GraphQL-bound のままにし、degraded state で `dependency: graphql-bound` と `unverified_fields` を示す。REST quota は `core` / `rest-core` である。 | `preview-through-1.x` | G674。surface ごとの call-site inventory と output-compatible adapter のみ。mutation、認証、`gh` replacement、cache、batch、budget、retry、sleep、reset scheduling、wake/supervision 変更、未検証 field の近似は追加しない。 |
| supervision escalation ladder と CI fallback | owner-subject finding は design の recorded transport を通じて 1 回だけ起こし、それ以外は owner を起こす。exact-head の settled-red は durable wait があり claimed/new-head repair がない場合だけ actionable、all-green は wait がなければ declared label を使える。 | `preview-through-1.x` | G657。broadcast、design recovery authority、terminal parsing、lifecycle mutation、release decision は追加しない。 |
| residency-resolved recipient delivery judgment | status、escalate、supervise が利用する 1 つの judgment。external reader は durable append（`recorded-reader-append`）、herdr resident は pane wake（`recorded-pane-wake`）で delivered になる。reader append failure は `.intent-cli/notify/<domain>/<team>/escalation-append-failures.jsonl` に保持する。 | `preview-through-1.x` | G660。reader event 成功時の false positive を止め、対応する open false positive を event history の rewrite なしに 1 回 clear する。failed-append finding と pane-resident の G641/G657 ladder は維持する。 |
| supervisor scheduler artifact | `.intent-cli/supervision/<domain>/<team>/install/intent-cli.supervise.<domain>.<team>.<platform-extension>` に team 固有の launchd plist、Task Scheduler XML、または systemd unit を emit し、明示的な current-session `reconcile|uninstall --write` が drift を報告・removal する。 | `preview-through-1.x` | G658/G712。artifact は `--write` のときだけ write する。install は registration を表示するだけで実行しない。macOS は login auto-load と reboot persistence を持たず、reconcile/uninstall は managed job を bootout し legacy login-persistent artifact を removal する。macOS から author した Windows / Linux output は `emitted-but-unverified`。liveness は引き続き `cycles.jsonl` の age と declared bound の比較である。 |
| supervision event waits | `cycles.jsonl` に `trigger: event` transition と `trigger: event-wait` の death / error + re-arm evidence を記録し、transition key は workspace / pane / `state_change_seq`。 | `preview-through-1.x` | G659。macOS 上の herdr 0.8.0 で実測し、他 version / platform は unverified。event / interval observation は 1 wake key を共有し、cycle liveness は独立 interval record を使う。adoption には G658 artifact の再生成と明示的な re-register が必要。 |
| runtime-state shape | automation が consume する runtime JSON state | `retain-through-1.x` | inventory のみ。automatic removal ではない。 |
| packet-schema variant | supported packet YAML/schema variant と state linkage | `retain-through-1.x` | future `retire-before-1.0` row は migration evidence を名指しする。 |
| field alias | documented compatibility field alias | `retain-through-1.x` | inventory のみ。inference や removal はしない。 |
| future eligible legacy retirement | consumer migration proof を持つ named legacy entry | `retire-before-1.0` | row に named migration evidence がなければこの disposition は無効。 |
| unattended-launch post-start interaction | `guide orchestrator-thread --format json` の structured recipe field（agent prompt、境界を維持する回答、`default_is_safe` flag、または interaction 未観測時の明示的な unmeasured absence） | `preview-through-1.x` | G636。v0.12.0 freeze 後に追加されたため 1.0 promise の対象外であり、後続 MAJOR で formalise する。 |
| per-kind launch recipe registry と `topology update-kind` surfacing | `guide orchestrator-thread --format json` の実測済み Copilot/Codex recipe entry。`update-kind` は変更とともに recorded recipe または明示的な absent notice を返す | `preview-through-1.x` | G647。Codex の fact は version/platform を付けた実測値だけを registry に置く。v0.12.0 freeze 後の追加であり、1.0 promise の対象外、後続 MAJOR で formalise する。 |
| workspace-layout guide | `guide workspace-layout` が operator の明示した workspace ID と 40% / 60% 均等の slot 規約、pane rename / resize command、一時 tab 往復を表示する。herdr command は実行しない | `preview-through-1.x` | G637。v0.12.0 freeze 後の追加であり、1.0 promise の対象外。後続 MAJOR で formalise する。 |
| post-freeze preview surface | v0.12.0 freeze 後に追加し、preview と明記した documented surface | `preview-through-1.x` | 1.0 promise の対象外。1.x で変更・撤回でき、後続 MAJOR で formalise する。 |

| scheduler runtime の自己完結性と cycle-level transport degradation | `notify supervise install` は `intent-cli` を絶対解決し、解決可能な runtime transport binary を絶対 path で出力し、未解決 binary と PATH を記録する。recipient liveness より前の transport process start failure は `transport-unavailable`、binary、error を持つ cycle-level `supervision-degraded` 一件になる。 | `preview-through-1.x` | G675。2026-08-12 の macOS node 08 における host/date/two-act attribution：launchd minimal-PATH の `/usr/bin/env intent-cli` loaded-but-silently-exiting/exit-127 と、4 accumulated supervisors の別 audit、さらに alive/mid-task recipient に対する `herdr` 欠落の 1 cycle 10 false losses。guidance は live PID と最初の `cycles.jsonl` record を確認する。G648 genuine-absence rule と G658 emit-only boundary は不変。duplicate detection、recovery sequence、新しい verdict/wake transport/emission target は範囲外。 |
| supervise install artifact hardening | `notify supervise install` は `bound < interval` を `bound-below-interval` として、構造的な `supervisor-not-running` の帰結を明示して拒否する。macOS artifact は `WorkingDirectory` と runtime 配下の `StandardOutPath` / `StandardErrorPath` を持ち、両 log path を名前付きで返す。宣言済み `--startup-bound` 内の writer 付き first cycle が success の条件であり、writer は後続 duplicate 比較用に記録する。duplicate finding は G699 backoff/park を使い observation-only のまま。 | `preview-through-1.x` | G704。artifact は emit-only で scheduler register や lifecycle action は行わず、first-cycle proof は terminal content ではない。Windows/Linux generation は follow-up。 |
| lane ごとの operator merge authority | `landing_mode: operator-merge` は approved + exact-head green 後に PR/lane/approval evidence を持つ `awaiting-operator-merge` を導出し、design へ 1 回通知し、人の merge 検知後は closeout-only continuation に変わる。 | `preview-through-1.x` | G678。待機は urge、remind、age-escalate されず、intent-cli の path は lane を merge しない。direct/default landing behavior、G668 routing snapshot、G669 propose/confirm は不変。 |
| Git-backed scope claim | `.intent-cli/claims/` 配下の immutable active record。scope は `execution-unit:<EU>` と `release-prep:<owner/repo>:<version>` のみ。attributed release/takeover history と detect-only `claim-stale` evidence を持つ。 | `preview-through-1.x` | G679; [ADR 0003](../adr/0003-git-push-cas-work-ownership.md)。plain push 成功が ownership fact。server、automatic expiry/takeover、heartbeat、queue/runs cross-clone 変更、追加 scope はない。fresh host は union rule 後に `.intent-cli/claims/** -merge` を追加し、existing host は exact guidance のみ。 |
| claim-aware work-start consumer | `claim verify`、packet draft、queue seed/publish-flow、worker next-action、next-slice、release-prep guidance が一つの read-only ownership judgment を参照する。claim-then-draft の番号は fresh base で再計算した次番号を exactly once retry する。 | `preview-through-1.x` | G680。store 設定時の unheld/other-team start は scope と holder を名指して拒否し、store 無しは byte-identical legacy behavior。next-slice は claimed-elsewhere を holder evidence 付きで除外。GitHub label は visibility/defence-in-depth のまま。G679 primitive、G669、publish content check、review/closeout、worker complete、queue merge semantics は不変。 |
| intent-cli host-state Git `index.lock` retry | sanctioned な claim transaction は intent-cli-owned host-state Git write に対する recognizable な `index.lock` contention だけを、宣言済み `max_attempts=4`、`window=2000ms`、jittered delay で retry する。成功は retry attempts を出し、terminal failure は elapsed time、original Git error、exact lock path、manual remediation を出す。 | `preview-through-1.x` | G700。lock は delete、rename、move、truncate、repair しない。non-lock Git error は retry しない。read-only Git と agent の free-form Git は対象外で、EN/JA guide と output marker は対応させる。 |
| domain-scoped design event stream | new write は `.intent-cli/events/<domain>/<team>.jsonl` を使い、read はその file を優先し、scoped file が absent の間だけ legacy `.intent-cli/events/<team>.jsonl` を使う。 | `preview-through-1.x` | G681。既存 external-reader topology value は edit なしで解決する。migration は正確な operator-owned move であり自動実行しない。event schema、delivery semantics、durable watermark、notify/supervision storage、reader replacement check は不変。 |
| host-local model resolution grammar と ledger | Codex/Claude entry は placeholder-only の model/effort flag grammar を示し、`.intent-cli/model-resolution/ledger.jsonl` は verified full invocation または error text 付き refused invocation を append して次回 launch 前に query する。表示された attempt ごとに対応する READY/refusal 記録 step が 1 回必須であり、miss は human fallback の前に read-only な `herdr agent list` と pane argv inspection を正確に表示する。 | `preview-through-1.x` | G685。解決順は ledger hit → currently-running same-kind argv → human への質問。model id/catalogue、provider launch/validation、cross-host sharing、G647 envelope 変更、G684 drift 変更はない。 |

## Guard

`CompatibilityPromiseG612Tests.Ledger_ContainsEveryRegisteredCommandAndRequiredLegacyDispositions_G612`
は existing dispatcher registry を reflection し、registered handler ごとに literal な
`group subcommand` ledger entry を要求します。local で row を削除すると test は
fail します。reflection は test 内にあり、CLI enumeration hook も runtime behaviour
も追加しません。

## Registered top-level alias

| Command | Machine contract / disposition |
| --- | --- |
| `improve` | documented な Guide JSON; `stable-at-1.0`。 |
| `improve record` | command-scoped Markdown/JSON の dry-run または append result。`preview-through-1.x`。 |
| `improve window` | command-scoped Markdown/JSON の dry-run または declared-window write result。`preview-through-1.x`。 |
| `grill` | documented な Guide JSON; `stable-at-1.0`。 |
| `stack` | documented な Guide JSON; `stable-at-1.0`。 |
| `next` | documented な Guide JSON; `stable-at-1.0`。 |
| `inspect` | documented な Guide JSON; `stable-at-1.0`。 |
