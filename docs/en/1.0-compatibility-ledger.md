# intent-cli 1.0 compatibility ledger

This reviewable ledger records the v0.11.1 machine-consumed baseline named by
the [1.0 compatibility promise](1.0-compatibility-promise.md). It is an
inventory, not a change to runtime behaviour.

## Reading a command row

Every row is a registered command/subcommand. Its command and flag names,
machine JSON field semantics and every machine-emitted `cause` value, exit-code
meaning, and referenced durable schema are covered under the 1.0 promise.
“Command-scoped JSON” deliberately does not invent a common envelope: a caller
uses that command's JSON shape and every `cause` value it emits;
a missing `cause` field does not acquire one by inference. The disposition is
the compatibility decision for the command surface.

## Cause-value coverage

Every `cause` value actually emitted in a machine-consumed JSON payload is covered, whether or not prose currently documents that value or this ledger lists it individually. Source emission, not prose documentation, determines coverage. For example, `topology-location-conflict` is covered because automation can receive it.

| Registered command | Machine JSON and cause vocabulary | Exit codes | Durable schema / disposition |
| --- | --- | --- | --- |
| `automation base-branch-check` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `automation check` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `automation clarification-stop` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `automation closeout-drift-check` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `automation complete` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `automation doctor` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `automation durable-state-preflight` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `automation heartbeat` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `automation host-loop-next-action` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `automation host-loop-wake` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `automation host-queue-item-recovery` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `automation host-review-diagnostics` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `automation host-review-preflight` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `automation host-sync-preflight` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `automation intent-target-gap-recovery` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `automation issue-block` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `automation issue-publish` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `automation issue-release` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `automation issue-retire` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `automation knowledge-writeback-record` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `automation label-palette-audit` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `automation label-palette-sync` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `automation pr-transition` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `automation publish-lifecycle-repair` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `automation publish-recovery` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `automation queue-dependency-reconcile` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `automation queue-seed-from-packet` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `automation reconcile` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `automation runs-audit` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `automation same-repo-metadata-preflight` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `automation stalled-work` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `automation state-doctor` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `automation summary` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `automation workspace-guard` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `bug implementation-issue` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `bug implementation-repair` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `bug plan` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `bug report` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `bug triage` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `clarification answer` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `clarification next` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `clarification status` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `clarify answer` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `clarify draft` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `clarify list` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `clarify open` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `clarify record` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `closeout pr` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `context collect` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `guide artifact-intake` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `guide automation` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `guide closeout` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `guide collaborate` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `guide commands` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `guide grill` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `guide help` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `guide host-ownership` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `guide improve` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `guide inspect` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `guide intent-work` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `guide interview-mode` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `guide interview-readiness` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `guide model` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `guide next` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `guide onboarding` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `guide oneshot` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `guide orchestrator-thread` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `guide prompt-matrix` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `guide prompt-template` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `guide question-style` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `guide review` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `guide review-verification-policy` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `guide rules` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `guide skill-pack` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `guide stack` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `guide start` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `guide worker` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `guide workflow` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `intent add-feature` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `intent analyze-tree` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `intent draft-from-interview` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `intent explain` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `intent facet-check` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `intent host-check` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `intent init` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `intent init-tree` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `intent lint-layout` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `intent next-slice` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `intent search` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `intent status` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `interview answer` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `interview compile` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `interview next-question` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `interview record-answer` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `interview resume` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `interview start` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `issue create` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `issue draft` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `issue plan-candidate` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `issue prepare` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `issue publish` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `issue publish-flow` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `issue publish-reviewed` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `issue status` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `issue validate-body` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `metadata update` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `metadata validate` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `migrate host-state` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `next-slice classify` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `notify delegate` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `notify escalate` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `notify report` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `notify status` | Command-scoped JSON when its documented `--format json` form is used; reports `dispatched_at`, recorded recipient identity, `recipient_running`, the named liveness source, `report_arrived`, and `verdict` (`live|settled|lost`); every machine-emitted `cause` value is covered. | `0` for a resolved status; nonzero when the pending store or liveness cannot be read. | See durable inventory below; `preview-through-1.x` |
| `judgment-wait open` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `judgment-wait query` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `judgment-wait resolve` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `judgment-wait supersede` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `operator-attention open` | Alias JSON is identical to `judgment-wait open` except for the structured `deprecation_warning` field naming replacement `judgment-wait`. | Same as `judgment-wait open`. | `deprecate-with-alias`; retained through 1.x, removal only next MAJOR. |
| `operator-attention query` | Alias JSON is identical to `judgment-wait query` except for the structured `deprecation_warning` field naming replacement `judgment-wait`. | Same as `judgment-wait query`. | `deprecate-with-alias`; retained through 1.x, removal only next MAJOR. |
| `operator-attention resolve` | Alias JSON is identical to `judgment-wait resolve` except for the structured `deprecation_warning` field naming replacement `judgment-wait`. | Same as `judgment-wait resolve`. | `deprecate-with-alias`; retained through 1.x, removal only next MAJOR. |
| `operator-attention supersede` | Alias JSON is identical to `judgment-wait supersede` except for the structured `deprecation_warning` field naming replacement `judgment-wait`. | Same as `judgment-wait supersede`. | `deprecate-with-alias`; retained through 1.x, removal only next MAJOR. |
| `packet draft` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `packet retire` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `project status` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `projection generate` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `projection regenerate` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `queue dispatch` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `queue enqueue` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `queue list` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `queue next` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `queue priority-drift` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `queue reprioritize` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `queue show` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `queue transition` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `review closeout-plan` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `review collect-signals` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `review signal-handled` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `safety nested-provider-handoff` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `session-layer marker` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `session-layer set` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `session-layer show` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `session-layer topology` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `skill diff` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `skill install` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `skill list` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `status brief` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `task fix-pr-comments` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `task issue-to-pr` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `task publish-next-issue` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `task review-pr` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `tasking ai-thread-summary-attach` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `tasking handoff` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `tasking handoff-bundle` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `tasking handoff-bundle-history` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `tasking handoff-bundle-import-dry-run` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `tasking handoff-bundle-inspect` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `tasking handoff-bundle-verify` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `tasking publish-reviewed-bridge` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `tasking task-packet` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `tasking task-packet-checklist` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `tasking task-packet-preview` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `worker claim` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `worker complete` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `worker issue-preflight` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `worker next-action` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `worker pr-comment-preflight` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `worker pr-review-preflight` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `worker result-summary` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |
| `worker signal` | Command-scoped JSON when its documented `--format json` form is used; every machine-emitted `cause` value is covered (no cross-command inference). | `0` for success; the command's documented nonzero validation/operational result otherwise. | See durable inventory below; `stable-at-1.0` |

## Durable schemas and legacy inventory

Post-freeze surfaces are not omitted from this ledger. A preview surface is
enumerated with `preview-through-1.x`; the same completeness guard that
requires every registered command to have a row also keeps preview surfaces
discoverable.

| Surface | Machine-consumed shape / states | Disposition | Evidence or rule |
| --- | --- | --- | --- |
| session-layer mode record | `.intent-cli/session-layer-mode.json`; recorded mode and transition history | `stable-at-1.0` | State transition semantics are covered. |
| per-team topology record | Canonical team topology record and its identity/path validation | `stable-at-1.0` | Record-first; no inferred mode change. |
| judgment-wait record | `.intent-cli/operator-attention.json`; `open` → `resolved` or `superseded` | `stable-at-1.0` | G623 names the general durable record; its on-disk format and identifiers remain unchanged. |
| operator-attention command alias | Alias for `judgment-wait`; no record schema or identifier change | `deprecate-with-alias` | Structured `deprecation_warning` names the replacement; alias is retained through 1.x and removal is only next MAJOR. |
| role-pane-mapping compatibility read | Removed legacy fixed-file read; legacy-only hosts fail closed with record/retire recovery commands | `retire-before-1.0` | Retirement evidence: `legacy-topology-retirements.jsonl` entry one (Zero4Racer); all four measured hosts are legacy-free. |
| pending delegation record | `.intent-cli/notify/<domain>/<team>/pending.jsonl`; append-only dispatch/report snapshots keyed by task id, with recipient identity, expected artifact, dispatch timestamp, and report resolution | `preview-through-1.x` | G629. Added after the v0.12.0 freeze; detection only, outside the 1.0 promise, with no supervision or re-dispatch. |
| runtime-state shapes | Runtime JSON state consumed by automation | `retain-through-1.x` | Inventory only; no automatic removal. |
| packet-schema variants | Supported packet YAML/schema variants and state linkage | `retain-through-1.x` | A future `retire-before-1.0` row must name migration evidence. |
| field aliases | Documented compatibility field aliases | `retain-through-1.x` | Inventory only; no inference or removal. |
| future eligible legacy retirement | A named legacy entry with its consumer migration proof | `retire-before-1.0` | This disposition is invalid without named migration evidence in its row. |
| unattended-launch post-start interaction | `guide orchestrator-thread --format json` recipe field recording the agent prompt, envelope-preserving answer, and `default_is_safe` flag | `preview-through-1.x` | G636; added after the v0.12.0 freeze, outside the 1.0 promise until a later MAJOR formalises it. |
| post-freeze preview surface | A documented surface added after the v0.12.0 freeze and explicitly marked preview | `preview-through-1.x` | Outside the 1.0 promise; may change or be withdrawn in 1.x; formalise at a later MAJOR. |

## Guard

`CompatibilityPromiseG612Tests.Ledger_ContainsEveryRegisteredCommandAndRequiredLegacyDispositions_G612`
reflects over the existing dispatcher registry and requires a literal
`group subcommand` ledger entry for every registered handler. Removing a row
locally makes that test fail. The reflection lives in the test, so it adds no
CLI enumeration hook and no runtime behaviour.

## Registered top-level aliases

| Command | Machine contract / disposition |
| --- | --- |
| `improve` | Guide JSON where documented; `stable-at-1.0`. |
| `grill` | Guide JSON where documented; `stable-at-1.0`. |
| `stack` | Guide JSON where documented; `stable-at-1.0`. |
| `next` | Guide JSON where documented; `stable-at-1.0`. |
| `inspect` | Guide JSON where documented; `stable-at-1.0`. |
